package com.example.demo.authentication;

/*
 * @Component
 * 
 * @RequiredArgsConstructor public class UserDetailsServiceImpl implements
 * UserDetailsService {
 * 
 *//** ユーザー情報テーブルRepository *//*
								 * private final UserInfoRepository repository;
								 * 
								 * @Override public UserDetails loadUserByUsername(String username) throws
								 * UsernameNotFoundException { var userInfo = repository.findById(username)
								 * .orElseThrow(() -> new UsernameNotFoundException(username));
								 * 
								 * return User.withUsername(userInfo.getLoginId())
								 * .password(userInfo.getPassword()) .roles("USER") .build(); } }
								 */
